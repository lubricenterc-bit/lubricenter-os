import { OrderWorkspace } from "@/components/order-workspace";
export default function NewOrderPage() {
  return <main className="container stack"><div><div className="eyebrow">ATENCIÓN · UNA VISITA, UNA ORDEN</div><h1>Nueva orden completa</h1><p className="muted">Cliente y vehículo → productos y trabajos → cobro. Todo desde aquí.</p></div><OrderWorkspace /></main>;
}

